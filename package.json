{
  "name": "gallery",
  "version": "1.0.0",
  "description": "Galeria w Bootstrap na szkoleniu w Altkom",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^0.12.0"
  }
}
